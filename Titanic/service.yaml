apiVersion: v1
kind: Service
metadata:
  name: titanic-api-service
spec:
  selector:
    app: titanic-api
  ports:
    - protocol: TCP
      port: 80         # Service port
      targetPort: 8000 # Container port
  type: NodePort    # Change to NodePort if you're using Minikube

