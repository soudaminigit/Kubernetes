KubernetesInstall.docx -> Contains instructions to run Kubernetes and create deployments and services
Each folder contains a service and includes set of files to run the corresponding service.
Deployments can be run using kubectl apply -f <name>.yaml
Services can be run using kubectl apply -f <name>.yaml
